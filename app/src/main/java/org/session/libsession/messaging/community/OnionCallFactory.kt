package org.session.libsession.messaging.community

import okhttp3.Call
import okhttp3.Request

class OnionCallFactory : Call.Factory {
    override fun newCall(request: Request): Call {
        TODO("Not yet implemented")
    }
}